@model DrivingSchoolWebApp.Services.Models.School.ChangeManagerSchoolViewModel

@{
    ViewData["Title"] = "ChangeManager";
}

<h2>ChangeManager</h2>

<h4>EditSchoolInputModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-area="Administration" asp-controller="ManageSchools" asp-action="ChangeManager" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input hidden="hidden" asp-for="Id" class="form-control" />
            <input hidden="hidden" asp-for="ManagerId"/>
            <div class="form-group">
                <label asp-for="NewManagerId" class="control-label"></label>
                <select asp-for="NewManagerId" 
                        asp-items="@(new SelectList(ViewBag.UsersIds,"Id","Username"))">
                    <option>Please select one</option>
                </select>
                <span asp-validation-for="NewManagerId" class="text-danger"></span>
            </div>
           <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

